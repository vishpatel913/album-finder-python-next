# OS / editor
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Node (frontend/ and functions/ keep their own too; this is a backstop)
node_modules/
dist/

# Env
.env
.env.local
.env.*.local

# Claude
.claude

# Album Finder runtime artefacts
/data/spotify_artist_cache.json
/data/spotify_album_cache.json
/data/*.xml
/data/.spotify-token-cache
/data/.spotify-user-token-cache
.cache

# Local Library.xml dumps — keep the folder, ignore its contents
/dumps/*
!/dumps/.gitkeep
